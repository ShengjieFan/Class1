def selectionsort( arr) :
        min_index=i
        for j in range(i+1,len( arr) ) :
             if(arr[min_index]>arr[j]):
                    min_index=j #找到最小值的索引
        temp=arr[min_index]
        arr[min_index]=arr[i]
        arr[i]=temp
    return( arr);
